# Project Run Requirements

## JDK Version
This project was built using **JDK 21** and requires JDK 17 or a later version to compile and run.

## VM Arguments
To run the program with assertions enabled as specified in the project requirements, the following VM argument must be used:
`-ea`
